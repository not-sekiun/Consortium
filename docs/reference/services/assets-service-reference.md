# Assets service

Assets are files that can be uploaded by clients to the server and then either
downloaded by other clients or used within the framework. Typically, the assets service
is used to make local files from clients available to remote agents.

The assets service is a particular instance of a repository service and as such has the
exact same API see [Repository Service Reference](repository-service-reference.md) for
more information.
